# Avisa (com um pedido de alteração) quando há versões novas das ações usadas na compilação.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "Compilação"
